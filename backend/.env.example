# .env.example - 환경 변수 템플릿
# 실제 사용 시 .env 파일로 복사하여 사용

# Firebase Service Account Key Path
# Firebase Console에서 서비스 계정 키(JSON) 다운로드 후
# JNext 루트 폴더에 'jnext-service-account.json'으로 저장
FIREBASE_CREDENTIALS_PATH=jnext-service-account.json
