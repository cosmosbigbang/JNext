# JNext 프로젝트 구조 변경 내역

작성일: 2026-01-16  
변경 사유: 확장성 확보 및 프로젝트별 분리

---

## 🔄 리팩터링 개요

### Before (기존 구조)
```
JNext/api/
├── analyze_hinobalance.py
├── category_theory_하이노골반.txt
├── exercise_하이노골반돌리기.txt
├── upload_hino_001.py
├── check_balance.py
├── test_chat_api.py
└── ... (70+ 파일 혼재)
```

**문제점**:
- HinoBalance 특화 파일과 범용 파일 구분 어려움
- Phase 2, 3 진행 시 더욱 복잡해질 우려
- 파일명만으로는 용도 파악 힘듦

---

### After (새 구조)
```
JNext/
├── api/                         # Django Backend
│   ├── api/
│   │   ├── ai_config.py
│   │   ├── views_v2.py
│   │   └── projects/
│   │       └── hinobalance.py   # 프로젝트 설정
│   ├── scripts/                 # 범용 유틸리티
│   │   ├── check/               # DB 상태 확인
│   │   ├── test/                # API 테스트
│   │   └── utils/               # 기타 도구
│   └── manage.py
│
└── projects/                    # 프로젝트별 분리
    └── hinobalance/
        ├── data/                # 원본 데이터
        │   ├── theory/          # 카테고리별 이론
        │   ├── exercises/       # 개별 운동
        │   └── combined/        # 통합 문서
        ├── scripts/             # 전용 스크립트
        │   ├── analyze.py
        │   ├── publishing.py
        │   ├── upload/
        │   └── organize/
        └── docs/
```

**장점**:
- ✅ 명확한 책임 분리
- ✅ 새 프로젝트 추가 용이
- ✅ 파일 검색 효율 향상
- ✅ Django 패턴 준수

---

## 📦 마이그레이션 상세

### Category A: 데이터 파일 (20개)
**From**: `api/*.txt`  
**To**: `projects/hinobalance/data/{theory, exercises, combined}/`

- 이론 파일: `category_theory_*.txt` → `data/theory/`
- 운동 파일: `exercise_*.txt` → `data/exercises/`
- 통합 문서: `theory_*.txt` → `data/combined/`

### Category B: HinoBalance 스크립트 (15개)
**From**: `api/*hino*.py`, `api/*balance*.py`  
**To**: `projects/hinobalance/scripts/`

- `analyze_hinobalance.py` → `scripts/analyze.py`
- `create_hinobalance.py` → `scripts/create.py`
- `publishing.py` → `scripts/publishing.py`
- `upload_hino_*.py` → `scripts/upload/`
- `organize_*.py` → `scripts/organize/`

### Category C: 범용 유틸리티 (40개)
**From**: `api/*.py`  
**To**: `api/scripts/{check, test, utils}/`

- `check_*.py` → `scripts/check/`
- `test_*.py` → `scripts/test/`
- 기타 → `scripts/utils/`

---

## 🔧 코드 수정 사항

### 1. Firebase 경로 수정
```python
# Before
cred = credentials.Certificate('../jnext-service-account.json')

# After
base_dir = Path(__file__).resolve().parent.parent.parent
cred_path = base_dir / 'jnext-service-account.json'
cred = credentials.Certificate(str(cred_path))
```

### 2. Django 설정 경로
```python
# Before
sys.path.append(os.path.dirname(os.path.abspath(__file__)))

# After
api_path = os.path.abspath(os.path.join(os.path.dirname(__file__), '..', '..', '..', 'api'))
sys.path.insert(0, api_path)
```

---

## ✅ 검증 완료

- [x] Django 시스템 체크 정상: `python manage.py check`
- [x] Git 커밋 완료: 79개 파일 이동 추적
- [x] 경로 수정 완료: publishing.py, organize/, fix_hino_titles.py
- [x] 문서 업데이트: README.md, 작업일정.md

---

## 🚀 향후 계획

### Phase 2 준비
- 새 프로젝트 추가 시: `projects/exam_navi/` 생성
- 동일 패턴 적용: `{data, scripts, docs}` 구조 유지

### 추가 개선 사항
- [ ] `api/projects/hinobalance.py` 활용 확대
- [ ] `projects/hinobalance/scripts/` 자동화 스크립트 문서화
- [ ] CI/CD 파이프라인 경로 업데이트 (배포 시)

---

**마이그레이션 완료**: 2026-01-16  
**Git Commit**: ddcbd52 "리팩터링: HinoBalance 폴더 기반 구조로 재편성"
